#include <stdio.h>
#include <algorithm>

int main(){
    int a, b;
    while(scanf("%d %d", &a, &b), a != -1){
        if(a - b <= -50 || a - b >= 50)
            printf("%d\n", (100 + a - b)%50);
        else
            printf("%d\n", abs((a - b)%50));
    }
}
