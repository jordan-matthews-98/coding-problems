#include <stdio.h>

int main() {
    int n;
    unsigned char *c;
    while(scanf("%d", &n) != EOF){
        printf("%d converts to ", n);
        c = &n;
        for(int i = 0; i < 4; ++i){
            n = n << 8;
            n += (unsigned char)v[i];
            v[i] = v[i] >> 8;
        }
        printf("%d\n", n);
    }
    return 0;
}
