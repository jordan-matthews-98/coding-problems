#include <stdio.h>

int main(){
    int T, L, W, H;
    scanf("%d", &T);
    for(int i = 1; i <= T; i++){
        scanf("%d %d %d", &L, &W, &H);
        if(L > 20 || W > 20 || H > 20)
            printf("Case %d: bad", i);
        else
            printf("Case %d: good", i);
    }
}
